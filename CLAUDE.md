# Real Estate Listing Video Generator

## 1. Project Overview

This project turns a realtor's listing photos and property details into a finished, branded marketing video automatically — no video editing software, no manual cutting.

The realtor uploads 4 property photos plus their own photo, picks a camera movement (Zoom In, Zoom Out, Pan Left, Pan Right) for each property photo, and fills in property details once. Within a few minutes the video is ready to download from the same frontend page: it opens with the 4 property photos, each first turned into a short AI-animated clip using the realtor's chosen movement, assembled with a lower third showing price, address, and bed/bath/sqft/year-built details, then closes with the realtor's uploaded photo, name, phone, and email. Looping background music plays underneath the full video.

**Why Creatomate + Higgsfield together, not Creatomate alone:** the original build shipped Creatomate-only (Ken Burns zoom/pan baked into the template), since chaining a separate AI animation API meant async job polling, retries, and a second failure surface — too much for a first deadline-driven build. That baseline proved out the rest of the pipeline (intake, Sheets lookup, Creatomate render, polling, delivery) before the animation upgrade landed on 2026-08-11: each property photo is now animated via Higgsfield (Kling v2.1 Master model — see §3 step 3 and §5) before reaching Creatomate, which still owns the lower thirds, transitions, background music, and final assembly. Creatomate no longer does the pan/zoom motion itself — that's baked into the clip Higgsfield generates.

## 2. The WAT Framework

This project is organized around **Workflows, Agent, Tools**:

- **W — Workflows** (`/workflows/`): step-by-step procedure files that describe *how* the pipeline runs — the process below. These are the source of truth for what should happen; Claude Code reads them before acting.
- **A — Agent**: Claude Code itself. No folder — it reads the workflow files, plans the work, and executes it using the Tools. When you ask for a change, expect the agent to check `/workflows/` first for the relevant procedure before touching anything.
- **T — Tools** (`/tools/`): scripts and integrations the agent calls to actually do things — n8n workflow JSON exports, Creatomate template helpers, Google Sheets helpers, deployment scripts. Tools should stay thin wrappers around n8n/Creatomate/Sheets, not reimplementations of logic that already lives in those systems.

## 3. The Pipeline (n8n workflow: "Listing Video Intake, Render & Poll")

1. **Realtor intake** — a webhook (the Vercel frontend, see §6) collects 4 property photos, a chosen camera movement per photo (`zoomIn`/`zoomOut`/`panLeft`/`panRight`), the realtor's own photo, price, address, bed/bath/sqft/year built, key features, description, and the agent's email. Responds immediately with a `submissionId`.
2. **Agent profile lookup** — Google Sheets node reads the Agents roster tab, matching by the submitter's email, so agents never re-enter their own name/phone/socials. (The realtor's photo now comes from the frontend upload, not this lookup — see §5.) If no match, the job is marked `agent_not_found` and stops there.
3. **Animate property photos (Higgsfield)** — once the agent is found, the Data Table row is marked `animating` and all 4 property photos are submitted to Higgsfield's image-to-video API **in parallel** (one shared poll loop checks all 4 in-flight jobs together each tick, not 4 sequential loops — keeps total wait time to roughly the slowest single clip instead of 4×). Model is `kling-v2-1-master`; each request sends the verbatim prompt matching the realtor's chosen movement plus a shared negative prompt — both live in `assets/higgsfield-real-estate-prompts.md`, which is the source of truth for the exact wording and its v1→v3 tuning history, not this doc. Poll cap is 50 attempts (~12.5 min) before marking the job `error`. If any single clip fails, gets `nsfw`-flagged, or the batch times out, the whole job is marked `error` and Creatomate is never called — see §5 for why `kling-v2-1-master` was chosen over other options and the credential/credit-pool gotchas.
4. **Send to Creatomate** — HTTP Request node fills the Creatomate template's real element names (`Video-01`/`Video-02`/`Video-03`/`Video-04` for the 4 animated clips from step 3, `Realtor-picture`, `Address`, `Details`, `Price`, `Description`, `Realtor-Name`, `Realtor-Phone`, `Realtor-Email`) and triggers a render. The template itself (built once in Creatomate's visual editor, not generated by code) is Project `Realtor-video-generator` (id `9050cc5b-154e-494a-b2a9-200833d8ae2e`), Template `48be3d34-9888-481c-8ebe-01778a49e255`. There's also a static `Title` element ("NEW LISTING") that isn't sent per-render — it's fixed template text. The template has no slot for the realtor's website/social handle; that field is currently dropped, not rendered anywhere.
   - **Resolved quirk — `Address`/`Price` element names in this template**: earlier, the element *named* `Address` was visually styled/positioned as the price display and the element *named* `Price` was visually the address display, so the HTTP node compensated by sending the price value under the `Address` key and the address value under the `Price` key (fixed 2026-07-23). On 2026-07-23, the realtor renamed the two Creatomate layers directly so each name now matches what it visually displays — `Address` shows the address, `Price` shows the price. The n8n HTTP node was updated to match (price → `Price` key, address → `Address` key), removing the old compensation. If price/address ever look swapped again in a render, check whether the Creatomate layer names have changed before touching the n8n node — the source of truth is now "does the key name match what the layer visually shows," not a baked-in swap.
   - **Resolved quirk — `Picture-01..04` → `Video-01..04`, `Realtor` → `Realtor-picture` (2026-08-11)**: when the template's photo elements were converted from Image to Video type (to accept the Higgsfield clips), the realtor also renamed them — caught only by inspecting the template's live layer list in the Creatomate editor, not from any prior doc. If a field mysteriously shows placeholder content again after a template edit, check the layer names directly before assuming the n8n payload is wrong — this template has drifted from its element names at least twice now.
5. **Poll for completion (Creatomate)** — Wait node + HTTP status check loop until the render is done, capped at ~40 attempts (~10 min) before marking the job `error`.
6. **Deliver** — n8n writes the finished video URL to the `ListingVideoJobs` Data Table row (see §5); the frontend, which has been polling that row via the status webhook, shows a **Download** button as soon as the URL appears. This is the primary delivery mechanism.
7. **Backup email** — once the Data Table row is marked `complete`, a Gmail node emails the matched agent (from the Sheets lookup, step 2) a download link for the video. This exists only as a fallback in case the realtor navigates away before the frontend shows the Download button — the frontend page is still the primary/expected delivery path. No email is sent on `error` or `agent_not_found`.

## 4. Folder Structure

```
/workflows/          step-by-step procedure files the agent follows
/tools/               scripts and integrations (n8n exports, Creatomate/Sheets helpers)
/frontend/            Next.js/React intake app, deployed to Vercel (see §6)
/temp/                scratch working space, gitignored
/temp/outputs/        rendered/downloaded video outputs, not deliverables
/temp/resources/      working copies of photos, templates, etc.
/assets/              reference material (original project doc, Higgsfield movement prompt templates)
.env                  API keys and secrets — gitignored, never commit
.gitignore
```

## 5. Tech Stack

- **n8n** — orchestrates the pipeline (intake webhook, Sheets lookup, Higgsfield animation + polling, HTTP calls to Creatomate, polling, status webhook, backup email). Two workflows: `Listing Video Intake, Render & Poll` and `Listing Video Status Check`.
- **Higgsfield** (image-to-video) — animates each property photo into a short clip before Creatomate assembles the final video (see §3 step 3). Model: `kling-v2-1-master` via Higgsfield's public API (`platform.higgsfield.ai/v1/image2video/kling`, body `params.model = "kling-v2-1-master"`). **Kling 3.0 Turbo — the original target — is still not reachable**, confirmed at every tier/spelling tried on Higgsfield's newer unified-path endpoint style (`platform.higgsfield.ai/kling-video/v3.0/{tier}/image-to-video`) as of 2026-08-14; it remains only accessible through Higgsfield's consumer app (bot-protected, session-authenticated — a confirmed dead end for server-to-server use). **Correction to an earlier conclusion**: it was previously documented here as "Kling 3.0 has no public API access at all" — that was wrong. `kling-video/v3.0/pro/image-to-video` **is** a real, reachable route on this account (confirmed via a genuine `not_enough_credits` response rather than `model_not_found`); the original "no access" conclusion was based only on the older `/v1/image2video/kling` endpoint's `model` enum (which only allows v2.1 variants) and never checked this separate newer-style endpoint. Kling 3.0 **Pro** is a real, untested-for-quality upgrade candidate for this step once credits allow — Turbo specifically is the only variant still out of reach. Chosen over plain `kling-v2-1` (better quality, ~1–10 min/clip vs ~1–2 min) and over Seedance (hallucinated a person, didn't reliably honor a custom prompt). Auth: `httpHeaderAuth` credential "Higgsfield API" (`Authorization: Key {ID}:{SECRET}`, values in root `.env` as `HIGGSFIELD_API_KEY_ID`/`HIGGSFIELD_API_KEY_SECRET`). **Higgsfield API credits are a separate pool from the consumer app's subscription credits** — top up at the Higgsfield Cloud dashboard (`cloud.higgsfield.ai`), not the app's own billing page; easy to confuse since they share a login. Movement prompts and their tuning history live in `assets/higgsfield-real-estate-prompts.md`.
- **Google Sheets** — Agents-roster only: spreadsheet "Real_Estate_Video_Generator" (id `1akzdENiWG3D4zu4zSLUv4B4xLrA98GO8-oDr1tA4uc4`), tab "Agents", one row per agent (`Email`, `Name`, `Phone`, `HeadshotUrl`, `Website`, `Instagram`), read-only lookup. `HeadshotUrl` is no longer used by the render (the realtor's photo now comes from a frontend upload instead) — the column can stay in the sheet unused or be removed, it doesn't matter.
- **n8n Data Table** — `ListingVideoJobs` (id `2j4dIhU0LEFKBFA8`): one row per submission (`submissionId`, `status`, `videoUrl`, `creatomateRenderId`, `agentEmail`, `errorMessage`, `pollAttempts`, `rawIntakePayload`), written and updated by n8n as each render progresses. Chosen over a second Google Sheets tab because it's written/updated multiple times per render and a native Data Table avoids Sheets' write-concurrency/rate-limit quirks.
- **Creatomate** — the render engine: lower thirds, transitions, background music, final video assembly. One template, built visually, reused per render. Pan/zoom motion on the property photos is no longer done here — it's baked into the clips Higgsfield generates (see the Higgsfield entry above); Creatomate just assembles the 4 already-animated clips plus text/photo overlays.
- **Gmail** (`gmailOAuth2` credential, "Gmail account") — sends the backup delivery email on render completion (see §3 step 6). OAuth2 credentials expire periodically; if the "Send Video Email" node starts failing with a "needs to be reconnected" error, reconnect it from n8n's Credentials list — this can't be done from the agent, it requires the browser OAuth flow.
- **Vercel** — hosts the frontend intake form (`/frontend/`), independent deploys from the n8n workflow.
- **Vercel Blob store access mode matters and can't be changed later**: when creating the Blob store, you must explicitly choose **Public** access — the dashboard defaults to and recommends **Private**, but a private store rejects the `access: 'public'` uploads this app needs (Creatomate has to fetch photo URLs directly, with no auth). Access mode is locked in at store creation; if a store ends up Private, the fix is deleting it and creating a new Public one, not a settings toggle.

## 6. Architecture: How the Frontend and Backend Talk to Each Other

```
Vercel (frontend)                          n8n (backend)
─────────────────                          ─────────────
1. POST intake data/photos  ───────────▶   Intake webhook
                                            → generates submissionId
   ◀─────────────────────────────────────  returns { submissionId }

2. n8n runs the pipeline (§3), writing/updating a row in the
   ListingVideoJobs Data Table as it goes: submitted → animating → rendering → complete/error

3. Frontend polls, using submissionId  ───▶ Status webhook
   every few seconds                         → reads matching Data Table row
   ◀─────────────────────────────────────  returns { status, videoUrl? }

4. Once status is "complete", the frontend shows a Download button
   using the returned videoUrl — the primary delivery path. n8n also
   emails the agent a backup download link at the same point (§3 step 6).
```

- **Two n8n webhooks/workflows**: `Listing Video Intake, Render & Poll` (starts the pipeline, returns a `submissionId` immediately) and `Listing Video Status Check` (reads the `ListingVideoJobs` Data Table row for a given `submissionId` and returns JSON). Both webhook URLs are stored as Vercel environment variables (`N8N_INTAKE_WEBHOOK_URL`, `N8N_STATUS_WEBHOOK_URL`) — never hardcoded in frontend code.
- **The `ListingVideoJobs` Data Table is the shared state** between the two systems — the intake workflow writes it, the status workflow reads it.
- **Delivery is frontend-first, email-backup**: the realtor gets the video by staying on the submission page and clicking Download once it's ready; a backup email with the same link goes out in parallel in case they navigate away. The frontend still shows a persistent "Don't close this window — your video is being generated" message for the whole pending/rendering state, since the email is a fallback, not the primary UX.

## 7. Frontend (`/frontend/`)

**What it needs to do:**
- Collect the same intake fields the n8n workflow expects: 4 property photo uploads (each paired with a camera-movement choice — Zoom In, Zoom Out, Pan Left, Pan Right), **the realtor's own photo upload**, price, address, bed/bath, sqft, year built, key features, description, and the agent's email (used for the Sheets lookup — name/phone/socials only, not the photo). Description is wired into the Creatomate render (`Description` element); key features remains frontend-only — the template has no slot for it.
- On submit, POST to the n8n intake webhook and receive a `submissionId`.
- Poll the n8n status webhook with that `submissionId`. While pending, show a persistent "Don't close this window — your video is being generated" message, with copy that distinguishes the `animating` stage (Higgsfield generating the 4 clips) from the `rendering` stage (Creatomate assembling the final video) — see `StatusPoller.tsx`. Once complete, show a **Download** button (linking to the video URL). This is the primary delivery mechanism; n8n also sends a backup email with the same link (see §3, §6) in case the realtor navigates away.
- Nothing else — no business logic, validation beyond basic form checks, or video processing happens client-side; that all stays in n8n + Creatomate.

**Where it lives:** `/frontend/` — a Next.js (App Router, TypeScript, Tailwind v4) app, deployed to Vercel independently of the n8n workflow. Built with the `frontend-design` skill for the visual/UX work — see §8 for the design direction.

**Photo uploads → Vercel Blob, not through the server:** the 5 photo files (4 property + realtor) are uploaded **directly from the browser** to Vercel Blob via `upload()` from `@vercel/blob/client`, authorized by a short-lived token minted by `app/api/blob-upload/route.ts`. This is deliberate, not incidental — routing real phone-camera photos (3–10MB each) through a Server Action or Route Handler risks Vercel's serverless body-size ceiling (Server Actions cap at 1MB by default; general function limits are ~4.5MB). Client-direct upload means file bytes never pass through a Next.js server function at all. Only after all 5 uploads resolve does the client call `submitIntake()` (a Server Action) with the resulting URLs.

**Conventions for the agent when generating or editing frontend code:**
- Keep `N8N_INTAKE_WEBHOOK_URL`, `N8N_STATUS_WEBHOOK_URL`, and `BLOB_READ_WRITE_TOKEN` in environment variables (`frontend/.env.local` locally, Vercel project settings in deployment) — never hardcode a webhook URL or token in committed code. `frontend/.env.example` documents all three.
- Prefer routing webhook calls through Next.js Route Handlers/Server Actions rather than calling n8n directly from client-side JS, so the webhook URLs aren't exposed in the browser bundle. The one exception is the Blob upload itself, which is intentionally client-direct (see above) — it doesn't touch the n8n webhooks.
- Don't duplicate pipeline logic (render triggering, status computation) in the frontend — it should only submit, poll, and display.
- The status proxy (`app/api/status/route.ts`) treats a 404 from the n8n status webhook as still-pending, not an error — the intake workflow responds with a `submissionId` in parallel with (not gated on) its Data Table row write, so a client can legitimately poll before that row exists.
- Use Vercel preview deployments to test form/UI changes before promoting to production.

## 8. Frontend Design Direction

The intake form and status page are deliberately styled as a two-act experience rather than a generic SaaS form:

- **Act I — the intake slip** (`/`): warm kraft-paper palette (`--paper`, `--paper-card`, `--ink`, `--stamp` red, `--brass`), a heavy poster slab serif (Alfa Slab One) for headlines, and a typewriter face (Special Elite) for labels/badges — evokes a real estate office's paper intake ticket, not an editorial cream-and-serif template. Signature element: a rotated rubber-stamp badge ("Listing Intake · File No. ####") that settles into place on load.
- **Act II — the screening room** (`/status/[submissionId]`): dark near-black palette with an amber "projector" accent, same typewriter face for the status readout. Signature element: a 12-tick film-leader countdown ring that glows while pending, replaced by a "Now Showing" panel with a one-time light-sweep animation on completion.
- Both scenes respect `prefers-reduced-motion` and have visible keyboard focus rings in their own accent color.
- Design tokens live in `frontend/src/app/globals.css` (`@theme inline` block); fonts are loaded via `next/font/google` in `frontend/src/app/layout.tsx`.

## 9. Implementation Status

- **n8n workflows built and ACTIVE**: `Listing Video Intake, Render & Poll` (id `BMyhNSqHqSUqgENA`) and `Listing Video Status Check` (id `ZWPqwUobRM0MRQ3J`), both in Tito's personal n8n project. **Both must stay toggled active** for their production webhook URLs to respond — this was a real gap caught late: all early testing used the MCP `execute_workflow` tool (manual test mode), which works regardless of active state, so the production webhook 404ing was only discovered once the real frontend called it directly.
- **n8n Data Table `ListingVideoJobs`** (id `2j4dIhU0LEFKBFA8`) created and wired into both workflows.
- **Google Sheets credential reconnected** and **Agents sheet created** (tab "Agents" in the "Real_Estate_Video_Generator" spreadsheet) — Agent Lookup is wired to the real spreadsheet/tab, not a placeholder.
- **Realtor photo comes from a frontend upload** (`realtorPhotoUrl` intake field), not a Sheets `HeadshotUrl` lookup.
- **Creatomate template's closing scene** has `Realtor-Name`, `Realtor-Phone`, and `Realtor-Email` text elements alongside `Realtor` (photo) — all wired into the render request. Creatomate credential (`httpBearerAuth`, "Creatomate API") attached to both HTTP nodes.
- **Template swapped 2026-07-22**: the render now points at the `Realtor-video-generator` template (`48be3d34-9888-481c-8ebe-01778a49e255`, project `9050cc5b-154e-494a-b2a9-200833d8ae2e`), replacing the original `c14aac66-5d48-4db4-946f-2fd792b44ff3`. Along with the element renames above, this added a 4th property photo slot (`Picture-04`, plumbed through the frontend as a `photo4Url` field) and started sending `Description`. The old `CTA-Text` (website/Instagram handle) has no equivalent in the new template and is no longer sent — the realtor's website/social handle doesn't currently appear in the video. A `Title` element ("NEW LISTING") exists but is static template text, not sent per-render.
- **Address/Price swap bug found and fixed 2026-07-23**: the first post-swap test render showed the price value in the address's visual slot and vice versa. Root cause was the template's own name-vs-visual-role mismatch (see §3) — fixed by swapping which value the HTTP node sends under the `Address` and `Price` keys. Re-verified with a second test render showing correct placement.
- **Address/Price fixed properly at the source, 2026-07-23**: the realtor opened the Creatomate template directly and confirmed the mismatch (the `Price`-named layer's text field held address content, the `Address`-named layer held price content) and renamed the two layers so each name now matches what it visually shows. This made the earlier HTTP-node compensation (above) backwards, so the `Trigger Creatomate Render` node was updated to send price → `Price` key and address → `Address` key (no swap) and the fix was published (`activeVersionId` updated) so the production webhook picks it up. Verify with a fresh test render before assuming this is settled.
- **Backup delivery email added 2026-07-23**: a "Send Video Email" Gmail node runs after `Mark Complete`, emailing the matched agent a download link using the existing `gmailOAuth2` "Gmail account" credential. Verified end-to-end (had to reconnect the credential once — it had expired). No email on `error`/`agent_not_found`.
- **Realtor-Name/Phone/Email typo fixed 2026-07-23**: the template's three closing-scene text layers were actually named `Realto-Name`/`Realto-Phone`/`Realto-Email` (missing the "r") — Creatomate silently falls back to an element's placeholder text when a modification key doesn't match any element name, so real Name/Phone/Email data was never showing, even though n8n was sending it correctly. Fixed by renaming the layers in Creatomate to match what n8n already sends. If a field mysteriously shows template placeholder text instead of real data again, check for an element-name mismatch like this before assuming the n8n payload is wrong.
- **Frontend built and deployed**: `/frontend/` is a working Next.js app — intake form (4 property-photo tiles, each with a movement selector) with client-direct Blob photo uploads, Server Action submit, status page with polling. `npm run build` passes. Live in production at `https://real-estate-video-generator-two.vercel.app`, connected to the `titoburgab/real-estate-video-generator` GitHub repo (Root Directory `frontend`, auto-deploys on push to `main`). Vercel project env vars (`BLOB_READ_WRITE_TOKEN`, `N8N_INTAKE_WEBHOOK_URL`, `N8N_STATUS_WEBHOOK_URL`) are set for Production.
- **Higgsfield image-to-video stage added 2026-08-11**: each property photo is now animated into a clip via Higgsfield before Creatomate — the realtor picks Zoom In/Out or Pan Left/Right per photo on the intake form (§7). New n8n nodes (`Mark Animating` → `Build Clip Jobs` → `Submit to Higgsfield` → shared poll loop (`Poll Delay - Higgsfield` / `Check Higgsfield Status` / `Increment Poll Count (HF)` / `Compute Terminal` / `All Clips Terminal?` / `Higgsfield Poll Cap Check`) → `Merge Clip Results` → `Any Clip Failed?`) inserted between `Agent Found?` and the existing `Trigger Creatomate Render`; everything downstream of Creatomate is otherwise unchanged. Model is `kling-v2-1-master` (see §5 for why, and why Kling 3.0 Turbo isn't usable here).
- **Movement prompts tuned through 3 versions (v1→v3), verified against real generated clips** — not just prompt review. v1 zoom moves rendered as a flat crop-and-scale with zero real parallax (confirmed by extracting and comparing actual video frames); v2's fix for that ("true 3D dolly, vanishing point must shift") overcorrected into visible warping and hallucinated geometry on this model; v3 settled on softer parallax language plus a much smaller pan-reveal distance. Full history, the final verbatim prompts, and the shared negative prompt are in `assets/higgsfield-real-estate-prompts.md` — treat that file, not this doc, as the source of truth if the prompts need tuning again.
- **Creatomate template updated for video inputs, 2026-08-11**: the `Picture-01..04` elements were converted from Image to Video type and renamed `Video-01..04`; the realtor's closing photo element turned out to be named `Realtor-picture`, not `Realtor` as earlier docs assumed — see §3 step 4's resolved-quirk note.
- **New `"animating"` status stage** added to the frontend (`JobStatus` union in `types.ts`, copy in `StatusPoller.tsx`) shown while Higgsfield is generating clips, before the existing `"rendering"` (Creatomate) stage.
- **Verified with a real end-to-end test, 2026-08-11**: a real webhook submission → Higgsfield generated 4 real clips (~6.5 min) → Creatomate assembled the final video (~20s) → `complete` status with a real downloadable video URL, no manual intervention. Two infra issues surfaced only by running the real test (both fixed): the `Google Sheets account` n8n OAuth2 credential had expired (same reconnect pattern as the documented Gmail credential issue above); and the workflow needed an explicit re-publish (`publish_workflow`) after the `update_workflow` edits, since the production webhook runs the last *published* version, not the latest saved draft — `active: true` alone doesn't mean the newest edits are live.
- **Movement prompts bumped to v4, 2026-08-14**: root cause found for Higgsfield seeming to "ignore" the old "camera starts zoomed in at approximately 35%" line — Kling's image-to-video conditioning anchors frame 0 to the literal input photo, unmodified, confirmed by extracting frame 0 from real generated clips. v4 rewrites all four prompts to start from the real framing instead of a fictitious pre-zoomed crop; Zoom Out was substantively redesigned (was quietly asking the model to hallucinate space beyond the photo's real edges — same failure mode already fixed for pans in v3). Only Zoom In was verified against real generated frames before shipping; Pan Right, Pan Left, and Zoom Out went out on reasoning alone because the Higgsfield API credit pool ran dry mid-test. Full history and the current verbatim prompts are in `assets/higgsfield-real-estate-prompts.md` — if pan/zoom-out clips look off, re-verify those three against real frames first.
- **Virtual staging (furnishing empty rooms before animating) explored 2026-08-14 — not currently viable.** Nano Banana Pro, the Higgsfield model suited for structure-preserving image edits, is not available on the current Higgsfield API plan (confirmed via `GET /models`, which lists only 13 models — DoP variants, Popcorn Auto, Soul variants, Soul ID — none of them image-editing models). As a fallback, tried asking `kling-v2-1-master` (the model already in production) to add furniture via prompt during animation — it produced a plausible-looking end frame, but on playback the furniture visibly overlays/dissolves onto the room rather than being anchored to its real geometry, confirming Kling isn't doing true structure-preserving edits. **Higgsfield support confirmed (2026-08-14) that the consumer app subscription (Plus/Ultra) is entirely separate from API access — upgrading it would not have unlocked Nano Banana Pro on the API.** The Cloud dashboard's own model catalog independently confirmed the same no-Nano-Banana-Pro finding. The one open question left is whether an Enterprise API plan specifically grants access — Higgsfield escalated that and a specialist follow-up was expected within 24h. No code changes were made for this — it was a pure feasibility spike, tracked here so it isn't re-attempted the same way without first checking on that Enterprise follow-up.

## 10. Conventions for the Agent

- Never commit `.env`, or anything under `/temp/outputs/` or `/temp/resources/` — these are gitignored scratch/output space, not deliverables.
- n8n workflow JSON exports live in `/workflows/` (`listing-video-intake-render-poll.json`, `listing-video-status-check.json`) — point-in-time snapshots for reference, not a live sync; re-export via the n8n MCP `get_workflow_details` tool after future pipeline edits so they don't go stale. The Creatomate-only baseline (pre-Higgsfield) was proven working before the AI-animation upgrade (§3 step 3) was merged in on 2026-08-11, per the original plan for this convention.
- Treat the Creatomate template and the n8n workflow as the source of truth for pipeline behavior — `/tools/` scripts should be thin wrappers around them, not reimplementations.
- Check `/workflows/` for the relevant procedure file before making pipeline changes; update the procedure file if the actual workflow changes.
- Movement prompt text for the Higgsfield step lives in `assets/higgsfield-real-estate-prompts.md` — that file, not this doc, is the source of truth for the exact wording. If it's tuned again, verify the fix against actual generated clip frames (not just prompt review) and bump its revision history, following the v1→v3 pattern already documented there.
- Before assuming an n8n modification key is wrong when a Creatomate field shows placeholder content, check the template's live layer names directly in the Creatomate editor first — this template's element names have drifted from what n8n sends at least three times now (`Address`/`Price` swap, `Realto-Name` typo, `Picture-01..04`→`Video-01..04`/`Realtor`→`Realtor-picture`).
- This is a foundation doc for a small, deadline-driven build — keep additions simple and avoid introducing new services/tools without flagging the tradeoff first.
